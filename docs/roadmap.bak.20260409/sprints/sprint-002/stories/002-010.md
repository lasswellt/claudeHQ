---
id: "002-010"
title: "Integration test — agent connects to Hub, starts session"
epic: "EPIC-009"
package: "hub+agent"
priority: 2
points: 3
dependencies: ["002-002", "002-006", "002-007"]
status: ready
assignee: "test-writer"
---

# 002-010: Integration test — agent to Hub round trip

## Context
The first end-to-end test: agent connects to Hub, registers, Hub sends session:start, agent spawns PTY (using `echo` as a stand-in for Claude), output flows back to Hub, session completes.

## Requirements
1. Start Hub in-process (not a separate process)
2. Connect agent WsClient to Hub
3. Verify agent:register creates a machine record
4. POST /api/sessions to start a session
5. Verify agent receives hub:session:start
6. Verify Hub receives agent:session:output and agent:session:ended
7. Verify session record in database shows completed status

## Acceptance Criteria
- [ ] Full round-trip test passes in vitest
- [ ] Uses `echo "hello"` as the command (not Claude Code)
- [ ] Session record created with status=running, then updated to completed
- [ ] Recording file created with output chunks
- [ ] Test runs in <10s

## Files Likely Affected
- `packages/hub/src/__tests__/integration.test.ts` (new)
