---
id: "002-002"
title: "Agent daemon process manager"
epic: "EPIC-004"
package: "agent"
priority: 1
points: 5
dependencies: ["002-001"]
status: ready
assignee: "agent-dev"
---

# 002-002: Agent daemon process manager

## Context
The daemon is the long-running process that ties everything together: WS client, PTY pool, config loading, and message routing. It receives commands from the Hub and dispatches them to the PTY pool.

## Requirements
1. `Daemon` class orchestrating WsClient + PtyPool
2. Load AgentConfig from `~/.chq/config.json` with env var overlay
3. Route Hub→Agent messages to appropriate handlers:
   - `hub:session:start` → pool.spawn()
   - `hub:session:kill` → pool.kill()
   - `hub:session:input` → pool.write()
4. Forward PtyPool events to Hub via WsClient:
   - `session:started` → send `agent:session:started`
   - `session:output` → send `agent:session:output`
   - `session:exit` → send `agent:session:ended`
5. Write Claude Code HTTP hooks config to `~/.claude/settings.json` on startup
6. Graceful shutdown: kill all sessions, close WS, exit

## Acceptance Criteria
- [ ] Daemon starts with config from file + env vars
- [ ] Receives `hub:session:start` and spawns PTY session
- [ ] PTY output forwarded to Hub as `agent:session:output`
- [ ] Session completion forwarded as `agent:session:ended`
- [ ] Hooks config written to `~/.claude/settings.json` before first session spawn
- [ ] SIGTERM triggers graceful shutdown

## Files Likely Affected
- `packages/agent/src/daemon.ts` (new)
- `packages/agent/src/hooks-config.ts` (new)
